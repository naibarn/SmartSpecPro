# Section 02 — Draft story contract

## Ownership

Preset synthesis and skill prompt contract; creative meaning remains skill-owned.

## Targets

- `apps/web/server/services/verticalDramaPresetSynthesis.ts`
- `apps/web/shared/verticalDramaSeries/storyControl.ts` only if an additive helper is needed
- both preset synthesizer skill copies and full-story architect skill
- synthesis/story-control tests

## Work

- Request optional `storyContext` and story-design fields in v1/v2 output shapes.
- Reuse validated Story Control seed for romance phases, pressure threads, and advantage
  beats; do not create a second unresolved-thread ledger.
- Add premise-derived coverage evidence for early payoff, pressure and romance engine when
  the premise contains them, without hardcoding these requirements for unrelated genres.
- Keep title language, narrative language and dialogue language ownership explicit.

## Acceptance

- The sample academic-romance premise produces a visible romance engine, scholarship risk,
  early math payoff and non-racism conflict guardrail.
- A one-preset or premise-only draft remains a reinterpretation, not a copied preset.
- Missing optional context remains compatible with old skill responses.
