# Decision Log

- Depth: `standard`
- Reason: architecture-heavy across team runtime, scheduling, summary, approval, and monitoring; still bounded enough to keep as a quick planning package.
- Core decision: keep persona reusable across multiple teams.
- Core decision: do not make persona itself the orchestrator runtime.
- Core decision: use team-member profile / runtime policy layers for orchestration behavior.
- Follow-on decision: the quick plan is promoted into the main planning spec at `planning/virtual-ai-office-orchestrator/` for full product coverage.
