# Section 02 — Pipeline Integration

Own the seam in `verticalDramaEpisodePipeline.ts` after storyboard generation and
before persistence. Build bounded prior-episode context with tenant/user/series
scope, invoke the semantic service once, apply the validated contract, and merge
credit metadata for repair candidates.

Reuse existing `screen_caller_refs`, `view_mode`/`dual_view`, supporting presence,
and downstream start-frame projection. Do not change authored synopsis or manual
cast authority. A review-required result must stop the stage before persistence
and before image-generation stages.
