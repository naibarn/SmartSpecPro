# Section 03 — Verification

Run focused unit/service/pipeline tests with mocked LLM responses, then skill
fixture verification and `git diff --check`. Inspect only task-owned diffs and
preserve the existing dirty worktree. Report any baseline typecheck noise
separately. Production deployment, browser replay, provider calls, and credit
spend remain unverified unless separately authorized.
