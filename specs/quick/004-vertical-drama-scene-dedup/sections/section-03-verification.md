# Section 03 — Verification

Run focused tests for location identity, special references, marketplace review selection, special episode input/recovery, and the dialog. Run `git diff --check` on owned paths. Attempt the web typecheck with a bounded wait and report baseline noise separately.
