# Request

Add an Admin-only `Add to Gallery` action to Document Management for image and
video Library items. The public Gallery media URL must be stable and must not
be a signed/provider URL that expires. Generic files remain unsupported.

Assumptions: reuse the existing Library Gallery publication service, link table,
and public Gallery media route; preserve unrelated dirty worktree changes.
