# Section 03: Verification

Run focused Library service/router and DocumentPreviewPanel tests, public
Gallery media route tests, `git diff --check`, and targeted TypeScript checks.
Report full workspace typecheck, browser, deployment, and production storage
verification separately if not performed.
