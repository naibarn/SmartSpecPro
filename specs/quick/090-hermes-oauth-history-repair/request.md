# Request

Fix the still-failing Hermes connection on Worker App 0.1.131 and make old
connection history compact through limits and collapse/expand behavior.

Constraints: retain per-connection credential isolation, never expose OAuth
secrets, preserve unrelated dirty work, support Thai and English, and deploy a
verified Windows installer.

