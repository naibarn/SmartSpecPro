# Decisions from conversation

No additional interview answers were collected. User requests a complete careful implementation plan. Accepted context: two additional engine options, JSON/SRT/VTT, word timings and script-based TTS alignment. Planning assumptions: preserve legacy default and ASS; VibeVoice opt-in; no automatic download/fallback; local first with explicit future cloud capability; Thai primary. Target GPU support and model quality are measurement gates, not inferred facts.
