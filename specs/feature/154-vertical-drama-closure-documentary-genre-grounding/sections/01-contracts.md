# Section 01 — Contracts

Additive shared contracts live in `apps/web/shared/verticalDramaSeries`. They
must use bounded Zod schemas at external boundaries and tolerant readers for
legacy JSON. `seriesFormat`, closure dispositions, and visual grounding are
versioned independently so one malformed optional block cannot discard the
whole story.
