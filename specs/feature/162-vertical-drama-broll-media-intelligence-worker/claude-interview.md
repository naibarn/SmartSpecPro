# Deep-plan interview — Feature 162

## Interview status

No blocking stakeholder question was required. The user supplied the domain
decisions in the spec and prior conversation: Worker-first local processing,
derived-only R2 publication, nine-shot storyboard integration, user-selectable
guided/AI-assisted/automated modes, MCP-primary ComfyUI, and typed start/reference
frame support.

## Auto-decisions

- Preserve source footage locally and make the verified derived artifact the
  only publishable media boundary.
- Reuse existing Worker auth/job/artifact seams and Vitest/Rust test patterns.
- Keep ComfyUI workflow selection server-resolved from an allowlist and record
  an immutable resolution snapshot per job.
- Treat MiniMax H3 as optional capability-probed workflow support, not a
  universal dependency.
- Implement in dependency order: shared contracts → native processing → server
  admission/publication/indexing → UI integration → integration proof.
