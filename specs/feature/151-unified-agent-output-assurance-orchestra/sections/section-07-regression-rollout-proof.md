# Section 07 — Regression and rollout proof

## Objective

Close the implementation with contract, adversarial, replay, budget, provider-boundary, future-scene, Agency-migration, and focused runtime regression coverage.

## Acceptance

All section tests pass; existing runtime tests remain green; no unrelated dirty files are staged. Rollout notes identify browser/provider/credit/deployment checks still required. Dependency upgrade is recorded with resolver/import/license/vulnerability evidence, or explicitly blocked with the exact conflict.

## Tests

Run the section-focused Python and Node suites, `git diff --check`, and targeted TypeScript checks. Do not claim real provider generation or authenticated browser proof from local tests.
