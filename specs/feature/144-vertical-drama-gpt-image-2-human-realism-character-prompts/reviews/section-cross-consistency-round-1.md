# SECTION CROSS-CONSISTENCY REVIEW — Round 1

## Sections reviewed

1. `section-01-capability-and-catalog`
2. `section-02-skill-and-generation-contract`
3. `section-03-character-render-normalizer`
4. `section-04-transport-persistence-and-compatibility`
5. `section-05-verification-and-ab-evaluation`

## Review result

| Check | Result | Notes |
|---|---|---|
| Interface alignment | PASS | Section 01 exports capability; Section 02 consumes facts and marker; Section 03 normalizes; Section 04 preserves the normalized shape. |
| Coverage gaps | PASS | Catalog, skill, normal/candidate generation, stale reuse, router branches, credit order, media/Hermes/MCP, persistence, tests, and A/B are covered. |
| Overlaps | PASS after fix | Capability tests belong to Section 01; request-normalizer tests belong to Section 03. The shared implementation module is sequenced 01 → 03, not edited in parallel. |
| Dependency order | PASS | No later section is required by an earlier section; Section 05 consumes all prior proofs. |
| Self-containment | PASS | Each section states scope, owned files, contracts, tests, failures, and exit criteria. |

## Interface checks

- `maxPromptChars` is the internal capability field and maps to the skill input
  field `max_prompt_chars`.
- `inline_only` means provider property absence, not an empty string; every
  transport section uses that same interpretation.
- `vd_character_natural_human_v1` is defined by Section 01 and consumed by
  Sections 02–05 without a conflicting spelling.
- `string.length` is the sole count semantics in capability, normalizer, and
  verification sections.
- Stale approved/candidate data is regenerated by the Section 02 generation
  service or rejected by Section 03; Section 04 never invents a third path.
- Candidate preflight occurs before claim/reservation where possible, and all
  sections preserve release/expiry behavior when claiming is required to load.

## Result

One ownership overlap was found and corrected by separating
`verticalDramaCharacterPromptContract.test.ts` from
`verticalDramaCharacterRequestNormalizer.test.ts`. No unresolved interface,
dependency, gap, or conflicting configuration remains.
