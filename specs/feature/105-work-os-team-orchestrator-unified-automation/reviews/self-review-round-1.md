# Self Review Round 1

## Findings

- The first draft treated workflow support as implicit, but the current automation surface schema does not expose `workflow` as a first-class route.
- The first draft mentioned skill improvement, but not `skill_studio` as a governed execution surface.

## Auto-fixes applied

- Added `workflow` and `skill_studio` as explicit planning targets in the spec, implementation plan, and section breakdown.
