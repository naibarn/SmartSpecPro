# Section 03 review

- Catalog rows carry opaque modelRef, Worker provenance, privacy, readiness, and
  task capability state.
- Explicit local selection queues only a pinned Worker job; general selectors use
  both existing catalog endpoints and media selectors remain isolated.
- Request idempotency collision and messages response extraction were fixed during review.
