# Section 05 review

- Worker App has provider/model CRUD and keyring secret actions; Web has model
  provenance/readiness display and existing owner sharing controls.
- General LLM selector data paths include all visible Worker rows; media selectors
  do not consume the Worker catalog.
- jsdom component proof passed; browser viewport evidence remains unverified by scope.
