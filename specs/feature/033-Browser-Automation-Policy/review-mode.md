self_review

Reason: Environment validation found no usable external review credentials (`GEMINI_API_KEY`/ADC unavailable and `OPENAI_API_KEY` unset), so the planning workflow must use self review automatically.
