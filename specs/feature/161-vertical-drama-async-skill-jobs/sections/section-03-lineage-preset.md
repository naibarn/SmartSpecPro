# Section 03 — Preset and lineage jobs

Convert legacy `synthesizeGenrePreset`, season carry-over, and special-edition brief routes to typed background jobs or the canonical draft composition queue when its schema fits. Preserve route authorization, wizard ownership, exact model selection, transient result persistence, and terminal error details. Do not leave a compatibility branch that awaits the LLM in the request.

Tests must prove submit latency, result persistence, refresh recovery, model identity, and explicit enqueue failure.
