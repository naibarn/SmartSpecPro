# Review Mode

- mode: `self_review`
- reason: External review credentials were unavailable during environment validation (`gemini_auth` missing and `OPENAI_API_KEY` unset), so mandatory automated review will run in self-review fallback mode.
