# Spec 209 Interview Record

No extra confirmation was requested because the user explicitly delegated the
full plan/implementation sequence.

## Confirmed product intent

- Prioritize important functions and make the system actually connected.
- Treat the attached/approved mockups as the UI source of truth.
- Do not invent a new screen design.
- Continue through implementation and repeated review without routine pauses.

## Auto-decisions

- AI Builder is the primary authoring entry; canvas is for understanding,
  inspection, debugging and small corrections.
- Build the core Builder → subflow/binding → Run/debug vertical slice before
  optional Marketplace breadth.
- Show readiness/setup-required/unavailable states rather than hiding missing
  dependencies or pretending a run is executable.
- Keep workflow definition, view layout and Mini App experience separate.

