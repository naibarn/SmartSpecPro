# Section 01 — Workflow Domain and Access

Create canonical Spec 209 semantic definition/version/view/app schema and
tenant-safe routers, mapping legacy workflow residue only through explicit
ownership/audit. Separate semantic graph from view coordinates and Mini App
schema. Tests cover draft/version/publish/rollback, ACL, immutable published
versions, schema validation and secret redaction.

