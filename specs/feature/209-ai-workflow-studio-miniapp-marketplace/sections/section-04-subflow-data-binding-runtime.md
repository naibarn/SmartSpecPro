# Section 04 — Subflow, Data Binding and Runtime Projection

Implement subflow breadcrumb/drill-down, explicit contract, compatible source
search and typed binding chips as shown in mockup 02. Validate ACL/types/cycles,
submit partial/single-node/subflow runs to Feature 195, and project checkpoints
and events while rejecting stale version/run updates. Tests cover bindings,
nested cycle, run-from, checkpoint/resume and stale event.

