self_review

Reason: Environment validation found no external review credentials (`gemini_auth` missing and `OPENAI_API_KEY` unset), so review falls back to mandatory self-review mode for this planning session.
