# Spec 208 Synthesized Specification

Implement a governed dynamic capability router that selects structured WebMCP,
browser semantic, local managed/existing browser, desktop Runner or visual
fallback per step. It must preserve tenant/auth/economic policy, require
approval for consequential effects, verify outcomes independently, and expose
honest session/blocked/recovery states.

