# Section 01 Code Review Interview Transcript

## Auto-fixes Applied (no user input needed)

### Fix 1: Cross-tenant group scope leakage (HIGH/security)
- **Issue:** group_members query in compute_effective_scopes() didn't filter by tenant_id
- **Fix:** Added JOIN user_groups and WHERE ug.tenant_id = :tenant_id AND ug.deleted_at IS NULL
- **Rationale:** Enterprise defense-in-depth per section plan

### Fix 2: Expired permissions included (HIGH/security)
- **Issue:** recompute_allowed_scopes() didn't check expires_at on library_permissions
- **Fix:** Added AND (expires_at IS NULL OR expires_at > NOW()) to permission query
- **Rationale:** Expired permissions must not grant access

### Fix 3: Soft-deleted items get scopes (HIGH/security)
- **Issue:** recompute_allowed_scopes() didn't check deleted_at on library_items
- **Fix:** Added AND deleted_at IS NULL to item query
- **Rationale:** Deleted items should not be accessible

### Fix 4: Unused imports (LOW/quality)
- **Issue:** `select` and `update` imported but not used in scope_engine.py
- **Fix:** Removed unused imports

## User Interview Decisions

### Decision 1: Hash algorithm for scope cache key
- **Question:** MD5 truncated to 8 chars (32 bits) has collision risk. Upgrade to SHA-256?
- **User answer:** Upgrade to SHA-256
- **Applied:** Changed to hashlib.sha256().hexdigest()[:16] (64 bits)

### Decision 2: Missing tenant_id behavior
- **Question:** Add warning log when retrieve() called without tenant_id?
- **User answer:** Implement fail-closed + warning/metric
- **Applied:** Returns empty RAGResult and logs warning when tenant_id is None
- **Updated tests:** Added tenant_id to all existing retrieve() calls, added test_missing_tenant_id_returns_empty

## Items Let Go (not blocking)

- #8: Migration bundled with unrelated presentation tables (auto-generated by drizzle-kit)
- #9: Cross-tenant test mocks away actual behavior (acknowledged mock limitation)
- #11: Duplicated test helper (will extract to conftest if pattern repeats in section 02+)
- #12: Cache tests assert string inequality (improved by checking actual engine._cache)
- #13: GIN index test only checks column existence (acceptable for unit tests)
- #6: No session flush (documented in docstring as caller responsibility)
