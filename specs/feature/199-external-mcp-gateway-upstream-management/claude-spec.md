# Feature 199 Synthesized Specification

Implement the External MCP Gateway described by `spec.md` over the shared 195–198 contracts. Manage upstream installations/connections and protocol/OAuth, discover tools lazily, quarantine risky/schema-changed tools, enforce tenant/role/agent policy, execute through capability and Job/Runner boundaries, project authorized context/RAG lineage and expose role-scoped UI.

Extend existing MCP registry/routes/routers/schema/UI and Python adapters. Do not duplicate connection/job/Runner/RAG/approval/asset/audit truth. Direct External Agent→arbitrary MCP and all retired execution paths remain prohibited.

