# Feature 198 Synthesized Specification

Implement the intelligent Chat/Universal Assistant control surface described by `spec.md` over Features 195–197. Route requests through governed Goal/Plan/Capability semantics, render source/plan/approval/task/result states, preserve retrieval provenance and build safe evolution/evaluation signals. Reuse existing Chat, help, RAG, MCP, runtime and UI patterns.

The implementation must be accessible, responsive, localized and browser-verifiable for `/chat`/Assistant, with bounded large-result artifactization, reconnect hydration, explicit degraded states and no policy bypass or retired dependency.

