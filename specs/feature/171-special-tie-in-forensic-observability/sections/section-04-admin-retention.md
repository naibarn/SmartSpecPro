# section-04-admin-retention

Expose admin-only bounded timeline/detail queries and best-effort expiry cleanup.
Keep raw payloads out of normal status APIs. Add limit, ownership, redaction, and
cleanup tests.
