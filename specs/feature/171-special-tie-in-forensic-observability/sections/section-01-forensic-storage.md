# section-01-forensic-storage

Add the additive forensic event schema/migration and service. Implement redaction,
hashing, bounded structured fields, best-effort writes, summary logging, exact
correlation filters, and cleanup. Write storage/redaction tests first.
