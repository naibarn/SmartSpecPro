# Section 01 Interview / Triage

No user interview was required.

Triage result:

- Auto-fix: kept WaveSpeed provider normalization and endpoint sanitization in a shared helper instead of duplicating logic in routers.
- Auto-fix: added direct tests for static fallback pricing tiers and endpoint safety.
- Let go: no optional tradeoff items required user input.
