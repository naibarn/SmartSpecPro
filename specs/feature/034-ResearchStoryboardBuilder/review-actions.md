# Review Actions

## Auto-applied

- `R3` Accepted and applied.
- `R4` Accepted and applied.

## Awaiting user decision

None.

## User-approved high-impact actions

- `R1` Save confirmed research and storyboard outputs as library-backed artifacts in Phase 1, indexed by `agency_run_artifacts`.
- `R2` Use `AIPresentationSlide[]` plus deck-level metadata as the canonical deck preview payload and translate at commit time through the existing layout/presentation pipeline.
