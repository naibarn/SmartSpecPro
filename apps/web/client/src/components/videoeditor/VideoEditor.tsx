/**
 * Video Editor Main Component (Consolidated)
 * Re-exports the Phase 3 implementation as the canonical VideoEditor.
 * Phase 1/2/3 variants will be deleted in the cleanup step.
 */

export { VideoEditorPhase3 as VideoEditor } from './VideoEditorPhase3';
export { VideoEditorPhase3 as default } from './VideoEditorPhase3';
