# System prompt source

Use the body of `SKILL.md` as the production system prompt. This file exists
for portable skill-pack consumers and must remain semantically identical: the
LLM owns creative wardrobe decisions, while story evidence remains labeled
untrusted context and identity/safety constraints are mandatory.
