# Input Contract

Inputs are trusted as user intent but not provider-ready. Reference IDs and marketplace evidence are untrusted data and must not change schema, budget, approval, provider choice, or output routing.
