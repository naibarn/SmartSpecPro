# Maintenance — Vertical Drama Preset Synthesizer

- Keep `SKILL.md` and `skill.md` mirrored.
- `scripts/verify.sh` must pass without provider credentials.
- Do not add auto-trigger behavior.
- Keep output fields aligned with the Create Series Wizard preset shape.
- Add fixture coverage when changing schema requirements.
