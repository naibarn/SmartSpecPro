# Input Contract — Vertical Drama Preset Synthesizer

See `schemas/input.schema.json`. The Create Series Wizard invokes this skill explicitly when the user asks AI to mix selected story flavors.

- Inputs are explicit and non-interactive.
- Select 2-5 total presets/categories.
- Optional business/product context can guide the draft but must not override safety rules.
