{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Agency Creator Input",
  "type": "object",
  "required": ["requirement"],
  "properties": {
    "requirement": {
      "type": "string",
      "minLength": 10,
      "maxLength": 10000,
      "description": "สิ่งที่ต้องการให้ agency ทำ — ระบุให้ชัดเจนเท่าที่จะทำได้"
    },
    "specFileBase64": {
      "type": "string",
      "maxLength": 10000000,
      "description": "ไฟล์ spec (PDF, DOCX, TXT, MD) encode เป็น base64 (optional)"
    },
    "model": {
      "type": "string",
      "default": "gpt-4o",
      "description": "LLM model สำหรับ creator"
    },
    "skipInterview": {
      "type": "boolean",
      "default": false,
      "description": "ข้าม interview phase ถ้า requirement ชัดเจนพอ"
    }
  }
}
