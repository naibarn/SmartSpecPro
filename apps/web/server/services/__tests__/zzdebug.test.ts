// Intentionally empty placeholder. This debug scratch file could not be
// deleted (rm denied by sandbox) — it is a leftover artifact from mocking
// path investigation and should be removed by the user or a follow-up
// commit. No test content; safe to ignore in CI.
import { describe, it } from "vitest";

describe.skip("zzdebug placeholder (safe to delete this file)", () => {
  it("noop", () => {});
});
