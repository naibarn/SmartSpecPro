# Worker App Sidecars

This directory is reserved for signed HyperFrames runtime sidecars used by the
Smart AI Hub Worker App.

The scaffold intentionally ships only placeholders. Release packaging must
replace them with allowlisted binaries and verify hashes/signatures before the
worker advertises HyperFrames readiness.
