# 🚀 Kilo CLI - Quick Start & Troubleshooting

## ✅ ขั้นตอนการใช้งานแบบย่อ

### 1. เปิด Backend (ทำครั้งเดียวต่อ session)
```bash
cd python-backend
source .venv/bin/activate
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

ตรวจสอบว่า Backend รันอยู่:
```bash
curl http://localhost:8000/health
```

### 2. เปิด Desktop App (ทำครั้งเดียวต่อ session)
```bash
cd desktop-app
npm run dev
```

เปิดเบราว์เซอร์: http://localhost:1420

### 3. ใช้งาน Kilo CLI
1. คลิก **"Kilo CLI (Compat)"** ทางซ้ายมือ
2. ใส่ **Proxy token**: `dev-token-smartspec-2026` → กด **Save Token**
3. ใส่ **Workspace**: `/home/naibarn/projects/SmartSpecPro`
4. กด **Refresh workflows** (รอ 1-2 วินาที)
5. เลือก workflow จาก dropdown หรือพิมพ์: `/test_hello.md`
6. กด **Run** หรือกด **Enter**

**หมายเหตุ:** Command ต้องขึ้นต้นด้วย `/` และลงท้ายด้วย `.md` (dropdown จะเพิ่มให้อัตโนมัติ)

---

## ❌ แก้ปัญหาเบื้องต้น

### ปัญหา 1: หน้าจอดำ ๆ ไม่มีอะไรเกิดขึ้น

**สาเหตุ:** Backend ยังไม่ได้รัน หรือ token ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ Backend:
   ```bash
   curl http://localhost:8000/health
   ```
   ถ้าไม่มี response → รัน Backend ใหม่

2. ตรวจสอบ Browser Console (กด F12):
   - มี error แดง ๆ หรือไม่?
   - มี 401/403 error หรือไม่? → token ผิด

3. ใส่ token ใหม่:
   - ใส่: `dev-token-smartspec-2026`
   - กด **Save Token**
   - จะขึ้น alert "Saved proxy token locally..."

### ปัญหา 2: กด Run แล้วไม่มีอะไรเกิดขึ้น

**สาเหตุ:** ไม่ได้ refresh workflows หรือไม่ได้ใส่ command

**วิธีแก้:**
1. กด **Refresh workflows** ก่อน
2. รอให้ dropdown มีรายการ workflows (1-2 วินาที)
3. ใส่ command: `/test_hello.md` (ต้องมี .md ท้าย)
4. กด **Run** หรือกด **Enter**
5. ดูพื้นที่สีดำ (Terminal) ด้านล่าง

### ปัญหา 3: ปุ่มเป็น text ธรรมดา ไม่สวย

**สาเหตุ:** Browser cache หรือ Desktop app ยังไม่ reload

**วิธีแก้:**
1. กด **Ctrl+Shift+R** (hard refresh)
2. หรือปิด browser แล้วเปิดใหม่
3. หรือ restart desktop app:
   ```bash
   # หยุด app (Ctrl+C)
   # รันใหม่
   npm run dev
   ```

### ปัญหา 4: ERROR "SMARTSPEC_PROXY_TOKEN is required"

**สาเหตุ:** Backend ไม่มี token ในไฟล์ `.env`

**วิธีแก้:**
1. เปิด `python-backend/.env`
2. ตรวจสอบว่ามีบรรทัดนี้:
   ```
   SMARTSPEC_PROXY_TOKEN=dev-token-smartspec-2026
   ```
3. ถ้าไม่มี → เพิ่มเข้าไป
4. Restart backend

### ปัญหา 5: ERROR "CORS origin not allowed"

**สาเหตุ:** Backend CORS config ไม่ถูกต้อง

**วิธีแก้:**
1. เปิด `python-backend/.env`
2. แก้บรรทัด CORS_ORIGINS เป็น:
   ```
   CORS_ORIGINS=["http://localhost:3000","http://localhost:1420","http://localhost:5173"]
   ```
3. Restart backend

### ปัญหา 6: ไม่มี workflows ให้เลือก

**สาเหตุ:** workspace ไม่มี `.smartspec/workflows/`

**วิธีแก้:**
1. ตรวจสอบว่า workspace path ถูกต้อง
2. ตรวจสอบว่ามีโฟลเดอร์:
   ```bash
   ls -la /home/naibarn/projects/SmartSpecPro/.smartspec/workflows/
   ```
3. ถ้าไม่มี → ใช้ workspace อื่น หรือสร้าง workflow ใหม่

---

## 🎯 ทดสอบว่าระบบทำงาน

### Test 1: Health Check
```bash
curl http://localhost:8000/health
```
**คาดหวัง:** `{"status":"healthy","version":"0.2.0",...}`

### Test 2: List Workflows
```bash
curl -H "Authorization: Bearer dev-token-smartspec-2026" \
  "http://localhost:8000/api/v1/kilo/list-workflows?workspace=/home/naibarn/projects/SmartSpecPro"
```
**คาดหวัง:** JSON list ของ workflows

### Test 3: Run Test Workflow ใน UI
1. ใน UI: ใส่ token + workspace
2. กด **Refresh workflows**
3. พิมพ์: `/test_hello.md` (ต้องมี .md ท้าย)
4. กด **Run** หรือกด **Enter**
5. ดูใน Terminal area ด้านล่าง

**คาดหวัง:**
```
=========================================
🎉 Kilo CLI ทำงานได้แล้ว!
=========================================

Workspace: /home/naibarn/projects/SmartSpecPro

รายการไฟล์ในโฟลเดอร์นี้:
total 123
drwxr-xr-x ...

=========================================
✅ Workflow สำเร็จ!
=========================================

[done] status=completed rc=0
```

---

## 📊 Status Display

ดู **Job ID / Status / Last Seq** ในกรอบสีเทาด้านล่าง command:

- **Job ID**: รหัส job ที่กำลังรัน (เช่น `kilo_abc123`)
- **Status**:
  - `starting` - กำลังเริ่ม
  - `running` - กำลังรัน (สีเขียว)
  - `done` / `completed` - เสร็จแล้ว (สีฟ้า)
  - `cancelled` - ถูกยกเลิก
- **Last Seq**: sequence number ของ output ล่าสุด

---

## 🔑 Keyboard Shortcuts

- **Enter** (ในช่อง Command) = Run workflow
- **Enter** (ในช่อง stdin input) = Send Input
- **Ctrl+K** = เปิด Command Palette

---

## 💡 เคล็ดลับ

1. **ใช้ Plan-only ก่อน Run**: กด "📝 Plan-only" เพื่อดูว่า workflow จะทำอะไร
2. **ดู History**: เปิด "History" ด้านล่างเพื่อดูคำสั่งที่เคยรัน คลิกเพื่อใช้ซ้ำ
3. **Test ง่าย ๆ ก่อน**: เริ่มจาก `/test_hello` ก่อนใช้ workflow ซับซ้อน
4. **ดู Console**: กด F12 ดู Console เพื่อดู error messages
5. **Refresh workflows**: ทำทุกครั้งที่เปลี่ยน workspace

---

## 🎮 ฟีเจอร์เพิ่มเติม

### ปุ่ม Cancel (⏹️)
- ใช้หยุด workflow ที่กำลังรันอยู่
- กดเมื่อต้องการหยุดการทำงาน

### ปุ่ม Clear (🗑️)
- ล้างหน้าจอ Terminal
- ใช้เมื่อต้องการเริ่มใหม่

### ปุ่ม Reconnect (🔌)
- เชื่อมต่อกลับหาก workflow ตัดการเชื่อมต่อ
- ใช้เมื่อหน้าจอหยุดแสดงผล

### Interactive Mode (stdin)
- ใช้สำหรับ workflow ที่ต้องการ input จากผู้ใช้
- เช่น ถามว่า "ต้องการดำเนินการต่อไหม? (y/n)"
- พิมพ์คำตอบแล้วกด **Enter** หรือ **📤 Send**

**ตัวอย่าง:**
```
Workflow กำลังรัน...
ถามว่า: "ต้องการสร้างไฟล์ใหม่? (y/n)"

→ ในช่อง stdin input พิมพ์: y
→ กด "📤 Send" หรือกด Enter
```

### Command Palette (📋)
- กด **📋 Command Palette** หรือ **Ctrl+K**
- ค้นหา workflow ได้ง่ายขึ้น
- เลือก workflow แล้วกด Apply

---

## 📞 ยังไม่ได้?

ถ้าทำตามทั้งหมดแล้วยังไม่ได้:

1. **เปิด Browser Console (F12)** → แชร์ error messages
2. **เปิด Backend terminal** → แชร์ logs
3. **ลองรัน curl test** (ด้านบน) → แชร์ผลลัพธ์
4. **ตรวจสอบว่า token ถูกบันทึก:**
   - กด F12 → Application/Storage → Local Storage
   - หา key: `smartspec.auth.proxyToken`

---

## 🔧 Debug Checklist

- [ ] Backend รันอยู่ที่ http://localhost:8000
- [ ] Desktop app รันอยู่ที่ http://localhost:1420
- [ ] ไฟล์ `.env` มี `SMARTSPEC_PROXY_TOKEN=dev-token-smartspec-2026`
- [ ] ใส่ token ใน UI และกด Save Token แล้ว
- [ ] ใส่ workspace path แล้ว
- [ ] กด Refresh workflows แล้ว
- [ ] เห็น dropdown มีรายการ workflows
- [ ] พิมพ์ `/test_hello.md` หรือเลือกจาก dropdown (จะเพิ่ม .md ให้อัตโนมัติ)
- [ ] กด Run
- [ ] ดูหน้าจอ Terminal ด้านล่าง

---

**Created:** 2026-01-08
**Version:** SmartSpec Pro 0.2.0
**Backend:** http://localhost:8000
**Desktop App:** http://localhost:1420
