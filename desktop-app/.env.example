# Desktop App (Tauri/Vite)

VITE_PY_BACKEND_URL=http://127.0.0.1:8000
VITE_WORKSPACE_PATH=

# IMPORTANT:
# - Do NOT bake secrets into the build for production.
# - Use the runtime "Proxy token" field in the UI (stored in OS keychain via Tauri).
# - Ensure python-backend enforces localhost-only + SMARTSPEC_PROXY_TOKEN in production.

# Legacy key-in-query is disabled by default (risky). Enable only for local dev.
VITE_ALLOW_LEGACY_KEY=0
VITE_ORCHESTRATOR_KEY=
