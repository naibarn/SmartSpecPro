server/services/__tests__/verticalDramaJudgedShotVideoPromptGeneration.test.ts > generateJudgedVerticalDramaShotVideoPrompt (Phase 2 — judged best-of-2 quality loop) > (d) judge returns invalid JSON on both attempts -> ships candidate A + warning, mode judged, verdict undefined
server/services/__tests__/verticalDramaJudgedShotVideoPromptGeneration.test.ts > generateJudgedVerticalDramaShotVideoPrompt (Phase 2 — judged best-of-2 quality loop) > (e) one candidate rejects -> survivor ships, mode single, judge never called
server/services/__tests__/verticalDramaJudgedShotVideoPromptGeneration.test.ts > generateJudgedVerticalDramaShotVideoPromptSpeakerSwitch (Phase 2 — full 6-scenario matrix, mirrors the non-split orchestrator exactly) > (d) judge returns invalid JSON on both attempts -> ships candidate A + warning, mode judged, verdict undefined
server/services/__tests__/verticalDramaJudgedShotVideoPromptGeneration.test.ts > generateJudgedVerticalDramaShotVideoPromptSpeakerSwitch (Phase 2 — full 6-scenario matrix, mirrors the non-split orchestrator exactly) > (e) one candidate rejects -> survivor ships, mode single, judge never invoked
server/services/__tests__/verticalDramaShotVideoPromptGeneration.test.ts > generateVerticalDramaShotVideoPrompt > throws VdSchemaValidationError (does not silently deduct credits) when BOTH attempts fail schema validation
