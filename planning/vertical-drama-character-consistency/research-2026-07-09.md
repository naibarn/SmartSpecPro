# Character Visual-Identity Consistency — Research (2026-07-09, task #27)

Read-only investigation: ตัวเลือกทำให้หน้าตา/ตัวตนตัวละครนิ่งข้ามช็อต/ตอน โดยใช้เฉพาะ
providers ที่ integrate แล้ว (คำสั่ง owner: report options+cost, no new infra without approval)

## ข้อค้นพบหลัก (มีหลักฐาน file:line ในรายงานเต็มของ agent)

1. **ระบบส่ง ref วันนี้ใช้ภาพเดียวเสมอ**: `getPrimaryPortraitUrl()`
   (verticalDramaCharacterStock.ts:283) อ่านเฉพาะ role `primary_portrait` LIMIT 1 —
   ทั้งที่ระบบ stock **เก็บ** `character_sheet_turnaround`/`character_sheet_full` ไว้แล้ว
   แต่ไม่เคยถูกอ่านกลับมาใช้ gen เลย → **ช่องว่างคือ resolver ไม่ใช่ data**
2. **กลไก consistency แบบ native มีอยู่แล้วในระบบแต่ vertical drama ไม่เคยแตะ**:
   Gemini Omni `character_ids` bank (wired ให้ Media Studio) และ Higgsfield
   `soul_cast` + role-tagged medias (wired ให้ Marketplace Capture) — VD ส่งแค่
   `image_urls` แบน ๆ ตลอด
3. `continuity_consistency` ใน quality review = มิติ**เนื้อเรื่อง** (text-only) ไม่ใช่
   การตรวจหน้าตาด้วยภาพ — อย่าเข้าใจผิดว่ามีแล้ว
4. kling-2.6 ช่วยไม่ได้ (maxReferenceImages:0, no start frame ใน catalog นี้)

## ตารางตัวเลือก

| ตัวเลือก | Effort | ต้นทุนเพิ่ม | ความเสี่ยง |
|---|---|---|---|
| **A. ส่ง ref ใบที่ 2/ตัวละคร (turnaround sheet ที่เก็บอยู่แล้ว)** | S (2-3 จุด: getPrimaryPortraitUrl → คืน ≤2, resolveShotCharacterReferenceUrls, ยังเคารพ maxReferenceImages เดิม) | $0 | ตัวละครเก่าอาจไม่มี sheet; กินโควตา ref ร่วมกับภาพสินค้า (merge เดิมให้ตัวละครชนะอยู่แล้ว) |
| B. เปลี่ยน default image model → google-banana-2 ("strong consistency") | S (config) | ×4 (10→40 เครดิต/ภาพ) | ยังไม่มีหลักฐาน A/B ในระบบ — **ต้องทดลองกับ 1 ซีรีส์ก่อน ห้าม default ทันที** |
| C. Wire Gemini Omni character_ids (ชั้นวิดีโอ) | M (คอลัมน์ map characterId→providerAssetId + wiring) | 90-300 เครดิต/คลิป (พอ ๆ หรือถูกกว่า veo3-lite default) | ช่วยเฉพาะชั้นวิดีโอ — drift จริงเกิดที่ start frame ซึ่งวิดีโอ inherit อยู่แล้ว |
| D. Higgsfield soul_cast role:"character" | M (manifest builder) | $0 ต่อ ledger เรา (บิลเข้า MCP ผู้ใช้) | ใช้ได้เฉพาะ user ที่ต่อ Higgsfield MCP |
| E. Fixed seed/ตัวละคร | S | $0 | ROI ต่ำ — seed ไม่รักษา identity ข้ามฉากบน API โฮสต์ |

## คำแนะนำ

1. **ทำ A ก่อน** — เล็ก ฟรี ใช้ของที่เก็บไว้แล้ว (implement เมื่อ episodes router ว่าง;
   จุดแก้: verticalDramaCharacterStock.ts resolver + resolveShotCharacterReferenceUrls
   ที่ verticalDramaEpisodes.ts:1134 + จุดเรียก :6227/:6499)
2. **B = รอ owner ตัดสินใจ** (ต้นทุน ×4) — เสนอทำ A/B ด้วยมือ 1 ซีรีส์เทียบกันก่อน
3. QC ตรวจหน้าซ้ำด้วย vision LLM: ทำได้ด้วยท่อเดิม (pattern
   verticalDramaVideoMotionPromptGeneration.ts:705 — ส่ง [portrait, ภาพช็อตใหม่] →
   {sameIdentity, confidence, driftedTraits[]}) แต่ไม่ deterministic + มีค่า call
   ต่อช็อตต่อตัวละคร → เสนอเป็น opt-in ระดับ series ไม่ใช่ default

## Collab-lite scoping (จาก audit จริง)

- วันนี้ series ผูก single userId + tenantId; role ระบบเป็น tenant-wide ใช้แทน
  per-resource ไม่ได้; ownership check กระจาย ~40 จุดใน episodes router (ไม่ centralized)
- precedent รูปแบบ: mediaProduction.accessPolicy.collaborators (JSON)
- **ชิ้นเล็กที่สุดที่ซื่อสัตย์: read-only signed share link** — ตารางใหม่
  vertical_drama_series_share_links (seriesId, token, scope, expiresAt) + procedure
  อ่านอย่างเดียว dedicated (เลี่ยงการ retrofit 40 จุด) — co-editor เต็มรูปเป็นเฟสแยก
- สถานะ: scoping only ตามคำสั่ง — ยังไม่ implement
