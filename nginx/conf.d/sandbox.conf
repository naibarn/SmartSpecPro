# Deprecated: sandbox.smartaihub.app is now defined in nginx/conf.d/dev-host.conf
# with both HTTP->HTTPS redirect and TLS settings. Keep this file as a placeholder
# so older setup scripts do not fail on a missing path.
