# 🎯 คู่มือใช้งาน Kilo CLI - ทีละขั้นตอน

## ✅ เตรียมความพร้อม

### 1. ตรวจสอบว่า Backend รันอยู่
```bash
curl http://localhost:8000/health
```
ถ้าได้ response แสดงว่า Backend ทำงานปกติ

### 2. ตรวจสอบว่า Desktop App รันอยู่
เปิดเบราว์เซอร์ไปที่: `http://localhost:1420`

---

## 📋 ขั้นตอนการใช้งาน Kilo CLI

### ขั้นที่ 1: เปิดหน้า Kilo CLI
1. เปิด Desktop App
2. คลิกที่เมนู **"Kilo CLI (Compat)"** ทางซ้ายมือ

### ขั้นที่ 2: ใส่ Proxy Token (ครั้งแรกเท่านั้น)
1. ในช่อง **"Proxy token"** ใส่: `dev-token-smartspec-2026`
2. กดปุ่ม **"Save Token"**
3. จะขึ้น alert "Saved proxy token locally..."
4. กด OK

**หมายเหตุ:** Token จะถูกบันทึกไว้ ไม่ต้องใส่ใหม่ทุกครั้ง

### ขั้นที่ 3: ใส่ Workspace Path
1. ในช่อง **"Workspace"** ใส่: `/home/naibarn/projects/SmartSpecPro`
2. หรือใช้ path ของโปรเจคอื่นก็ได้

### ขั้นที่ 4: โหลดรายการ Workflows
1. กดปุ่ม **"Refresh workflows"**
2. รอสักครู่ (1-2 วินาที)
3. dropdown "(autocomplete workflows)" จะมีรายการ workflows ให้เลือก

### ขั้นที่ 5: เลือก Workflow ที่ต้องการรัน

**วิธีที่ 1: เลือกจาก Dropdown**
- คลิกที่ dropdown "(autocomplete workflows)"
- เลือก workflow เช่น `test_hello`
- คำสั่งจะถูกใส่ในช่อง "Command" อัตโนมัติ

**วิธีที่ 2: พิมพ์เอง**
- ในช่อง **"Command"** พิมพ์ (ต้องมี .md ท้าย):
  ```
  /test_hello.md
  ```
  หรือ workflow อื่นๆ เช่น:
  ```
  /smartspec_validate_index.md
  /smartspec_generate_spec.md
  /autopilot_status.md
  ```

**หมายเหตุ:** Command ต้องขึ้นต้นด้วย `/` และลงท้ายด้วย `.md`

### ขั้นที่ 6: รัน Workflow
1. กดปุ่ม **"Plan-only"** (ดูแผนก่อน - แนะนำสำหรับครั้งแรก)
   หรือ
2. กดปุ่ม **"Run"** (รันจริง)

### ขั้นที่ 7: ดูผลลัพธ์
- ผลลัพธ์จะแสดงในพื้นที่ **Terminal** สีดำด้านล่าง
- จะเห็น output แบบ real-time
- รอจนกว่าจะเห็น `[done] status=...`

---

## 🎮 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: ทดสอบด้วย test_hello

1. ใส่ token (ถ้ายังไม่ได้ใส่)
2. ใส่ workspace: `/home/naibarn/projects/SmartSpecPro`
3. กด "Refresh workflows"
4. ในช่อง Command พิมพ์: `/test_hello.md` (หรือกดปุ่ม "/test_hello")
5. กด "Run" หรือกด Enter
6. ดูผลลัพธ์ในหน้าจอ Terminal

**ผลลัพธ์ที่คาดหวัง:**
```
=========================================
🎉 Kilo CLI ทำงานได้แล้ว!
=========================================

Workspace: /home/naibarn/projects/SmartSpecPro

รายการไฟล์ในโฟลเดอร์นี้:
total 123
drwxr-xr-x ...
...

=========================================
✅ Workflow สำเร็จ!
=========================================

[done] status=completed rc=0
```

### ตัวอย่างที่ 2: ตรวจสอบ SPEC Index

1. Command: `/smartspec_validate_index.md`
2. กด "Plan-only" เพื่อดูแผน
3. ถ้าโอเค กด "Run"

### ตัวอย่างที่ 3: Autopilot Status

1. Command: `/autopilot_status.md --platform kilo`
2. กด "Run"
3. ดูสถานะโปรเจค

---

## 🔧 ฟีเจอร์เพิ่มเติม

### ปุ่ม Cancel
- ใช้หยุด workflow ที่กำลังรันอยู่
- กดเมื่อต้องการหยุดการทำงาน

### ปุ่ม Clear
- ล้างหน้าจอ Terminal
- ใช้เมื่อต้องการเริ่มใหม่

### ปุ่ม Reconnect
- เชื่อมต่อกลับหาก workflow ตัดการเชื่อมต่อ
- ใช้เมื่อหน้าจอหยุดแสดงผล

### History
- เปิด "History" ด้านล่างเพื่อดูคำสั่งที่เคยรัน
- คลิกเพื่อใช้คำสั่งซ้ำ

### Interactive Mode (stdin)
- ใช้สำหรับ workflow ที่ต้องการ input
- เช่น ถามว่า "ต้องการดำเนินการต่อไหม? (y/n)"
- พิมพ์คำตอบแล้วกด Enter หรือ "Send"

**ตัวอย่าง:**
```
Workflow กำลังรัน...
ถามว่า: "ต้องการสร้างไฟล์ใหม่? (y/n)"

→ ในช่อง stdin input พิมพ์: y
→ กด "Send"
```

---

## ❌ ข้อผิดพลาดที่พบบ่อย

### 1. "SMARTSPEC_PROXY_TOKEN is required"
**สาเหตุ:** ยังไม่ได้ใส่ token หรือ token ผิด

**แก้ไข:**
- ใส่ token: `dev-token-smartspec-2026`
- กด "Save Token"

### 2. หน้าจอ Terminal ว่างเปล่า
**สาเหตุ:** ยังไม่ได้รัน workflow

**แก้ไข:**
- ตรวจสอบว่าใส่ token แล้ว
- ตรวจสอบว่าใส่ workspace แล้ว
- กด "Refresh workflows"
- เลือก workflow และกด "Run"

### 3. "job_not_found"
**สาเหตุ:** Backend ไม่พบ job

**แก้ไข:**
- รัน workflow ใหม่
- กด "Clear" แล้วรันใหม่

### 4. ไม่มี workflows ให้เลือก
**สาเหตุ:** workspace ไม่มี `.smartspec/workflows/`

**แก้ไข:**
- ตรวจสอบว่า workspace path ถูกต้อง
- ตรวจสอบว่ามีโฟลเดอร์ `.smartspec/workflows/` อยู่

---

## 📚 Workflows ที่มี (73 workflows)

### 🔧 Workflow ทั่วไป
- `test_hello` - ทดสอบระบบ (ง่ายที่สุด)
- `smartspec_validate_index` - ตรวจสอบ SPEC Index
- `smartspec_generate_spec` - สร้าง SPEC document
- `smartspec_generate_tasks` - สร้าง tasks.md
- `smartspec_generate_tests` - สร้าง unit tests

### 🤖 Autopilot
- `autopilot_ask` - ถามคำถาม
- `autopilot_run` - รัน workflow ถัดไป
- `autopilot_status` - ดูสถานะ

### 🎨 UI/UX
- `smartspec_generate_ui_spec` - สร้าง UI spec
- `smartspec_ui_validation` - ตรวจสอบ UI
- `smartspec_ui_accessibility_audit` - ตรวจสอบ accessibility

### 🔐 Security
- `smartspec_security_threat_modeler` - วิเคราะห์ภัยคุกคาม
- `smartspec_security_audit_reporter` - รายงานความปลอดภัย

### 📊 Performance
- `smartspec_performance_profiler` - วิเคราะห์ performance
- `smartspec_nfr_perf_planner` - วางแผน performance

---

## 💡 เทคนิคการใช้งาน

### 1. ใช้ Plan-only ก่อน Run
- กด "Plan-only" เพื่อดูว่า workflow จะทำอะไร
- ตรวจสอบว่าถูกต้อง
- ค่อยกด "Run"

### 2. ใช้ History
- เปิด "History" เพื่อดูคำสั่งที่เคยรัน
- คลิกเพื่อใช้ซ้ำ

### 3. ใช้ Command Palette
- กด "Command Palette" หรือ Ctrl+K
- ค้นหา workflow ได้ง่ายขึ้น

### 4. ตรวจสอบ Status
- ดู jobId, status, lastSeq ด้านล่าง Command
- เพื่อทราบสถานะการทำงาน

---

## 🎯 สรุป

**ขั้นตอนย่อ:**
1. ✅ ใส่ Token → Save
2. ✅ ใส่ Workspace
3. ✅ Refresh workflows
4. ✅ เลือก workflow หรือพิมพ์ command
5. ✅ กด Run
6. ✅ ดูผลลัพธ์

**ทดสอบครั้งแรก:** ใช้ `/test_hello` เพื่อทดสอบว่าระบบทำงานปกติ

**เมื่อเข้าใจแล้ว:** ลองใช้ workflows อื่นๆ เช่น `/smartspec_validate_index`

---

**Created:** 2026-01-08
**สำหรับ:** SmartSpec Pro v0.2.0
**Backend:** http://localhost:8000
**Desktop App:** http://localhost:1420
