[pytest]
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
markers =
    unit: Unit tests
    integration: Integration tests
    e2e: End-to-end tests
    slow: Slow tests
    auth: Authentication tests
    payments: Payment tests
    dashboard: Dashboard tests
    llm: LLM proxy tests
    credits: Credit system tests
addopts =
    --verbose
    --strict-markers
    --tb=short
    --cov=app
    --cov-report=term-missing
    --cov-report=xml:coverage.xml
    --cov-fail-under=20
    --ignore=tests/unit/test_rate_limit_service.py
    --ignore=tests/services/test_rate_limit_service.py
    -ra
asyncio_mode = auto
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
