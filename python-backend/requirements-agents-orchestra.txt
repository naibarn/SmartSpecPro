# Isolated Orchestra dependency profile.
#
# The Agents Orchestra runtime is part of requirements.txt. This compatibility
# manifest is intentionally dependency-free so alternate build paths cannot
# reintroduce a second, conflicting SDK pin.
