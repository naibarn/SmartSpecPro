# SmartSpec Autopilot Requirements
# Pinned versions for security and reproducibility

# Core dependencies
langgraph>=0.2.0,<0.3.0
langgraph-checkpoint>=0.2.0,<0.3.0

# Optional but recommended
pyyaml>=6.0,<7.0
typer>=0.9.0,<1.0.0

# For testing (optional)
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0

# Last updated: 2025-12-26
# To install: pip3 install -r requirements.txt
