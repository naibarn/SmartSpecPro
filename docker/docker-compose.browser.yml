services:
  browser-sandbox:
    image: smartspec/browser-sandbox:local
    build:
      context: ./browser-sandbox
    init: true
    ipc: host
    security_opt:
      - seccomp:./browser-sandbox/seccomp-chromium.json
      - no-new-privileges:true
    cap_drop:
      - ALL
    # SYS_ADMIN intentionally NOT added -- use --disable-setuid-sandbox in Chromium flags
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
    networks:
      - browser-isolated
    # No port exposure -- accessed only via Docker network from python-backend
    restart: "no"

networks:
  browser-isolated:
    driver: bridge
    internal: true  # No outbound internet by default
