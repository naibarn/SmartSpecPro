# Contracts

- `frame_analysis` is the source of truth for a speaker's screen-position bucket.
- A prompt position that contradicts that bucket cannot be persisted after the single corrective retry.
- The quality judge receives the start frame followed by the labeled character portraits.
