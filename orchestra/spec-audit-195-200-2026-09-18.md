# Feature 195-200 implementation audit — 2026-09-18

## Result

The six implementation configurations are structurally complete at 6/6
sections each. A fresh 20-round executable audit passed for every spec. The
audit used the section checker, UI-contract checker, Feature 186 producer
boundary verifier, focused cross-spec Vitest suite, and `git diff --check` in
each round.

Evidence from the final focused run:

- 11 Vitest files passed; 80 tests passed and 2 were skipped.
- Python Feature 186 vector scheduling boundary: 1 passed.
- Feature 186 verifier: `ok: true`, `directTransportCallSites: 0`,
  `unmigratedSideEffectingCallSites: 0`.
- 20/20 audit rounds completed with `all_20_rounds_ok`.
- No repository-wide TypeScript type-check was run because of the documented
  memory constraint.

## Gaps closed during this audit

- Feature 195 now enforces the canonical status transition table at the default
  repository update boundary. Job dependencies are checked before claim;
  dependents wait for successful prerequisites and fail closed with operator
  review when a prerequisite is missing or terminally failed.
- Feature 196 now carries an explicit `offerId` and `selectedOfferId`,
  revalidates the plan hash before approval/handoff, calculates policy cost from
  selected Offers, preserves independent per-step idempotency, and converts
  Plan-step dependencies into predecessor Job IDs.
- Feature 197 now validates and normalizes Runner identity/snapshot/list data,
  rejects duplicate entries and invalid snapshot windows, and fails closed for
  malformed work-offer/path inputs.
- Feature 199 now validates bounded MCP request identifiers and plain argument
  objects and rejects malformed grant expiry data before durable Job creation.
- Feature 200 now validates bounded manifest identity/list fields and malformed
  event payloads without allowing credential-bearing data through.
- Feature 198 was rechecked as the projection boundary: Chat remains a view of
  canonical Job state and does not create a second execution authority.

## Cross-spec ownership and non-duplication

| Concern                                                     | Owner | Boundary                                     |
| ----------------------------------------------------------- | ----- | -------------------------------------------- |
| Job lifecycle, lease, outbox, dependency claim              | 195   | Canonical `worker_jobs` control plane        |
| Command, Goal, Plan, Offer selection and approval           | 196   | Provider-neutral plan handed to 195          |
| Runner identity, capability freshness and offer eligibility | 197   | Eligibility only; lease remains 195          |
| Chat request/task projection                                | 198   | `/chat` view/command boundary; no Job ledger |
| MCP discovery/grant/effect-time governance                  | 199   | Durable `mcp.tool.execute` Job boundary      |
| External coding-agent manifest/provider adapter boundary    | 200   | Durable `external_agent_task` Job boundary   |

## Remaining activation gates

These are not silently marked complete because they require runtime, target
account, product or browser evidence that is not available in the local
contract tests:

- Feature 196 Goal/Plan durable persistence and plan/approval UI integration.
- Feature 197 authenticated desktop discovery, durable reconnect journal and
  connected Runner/browser acceptance.
- Feature 198 plan cards, live-task UI and consented evolution persistence.
- Feature 199 remote discovery/OAuth lifecycle, endpoint observability and
  catalog/quarantine UI.
- Feature 200 Codex/Claude/Antigravity/DeepSeek process adapters, connected
  Runner/Cloudflare runtime result projection and Agent UI.
- Feature 186 production gates reported by its verifier: legacy compatibility
  retirement, recovery/PITR/deployment rehearsal, tenant-transfer enablement,
  and Cloudflare target-account proof.

No new caller, route, migration or compatibility path was added for retired
Agency, legacy workflow, workpacks, OpenSandbox, Docker or `sandbox_jobs`
systems. Existing unrelated dirty-worktree changes were preserved.
