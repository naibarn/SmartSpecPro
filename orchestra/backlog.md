# Orchestra Backlog

- Consider persisting the exact per-slide canonical article chunk as dedicated metadata if future tooling needs explicit audit/debug visibility separate from `slide.notes`
- Consider revisiting article split prompting later to remove now-redundant free-form `notes` generation for article-driven drafts and reduce wasted tokens
