# Orchestra Backlog

- Optional manual QA: load `apps/extension/dist` in Chrome, open the Production tab, select a project, and drag reference/start/stop images into the target browser page to verify the receiving site accepts external drag URLs.
