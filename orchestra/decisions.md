# Orchestra Decisions Log

## 2026-04-26 — Start Skill System vNext
Risk: MEDIUM
Mode: auto_by_default

- Archived stale Server Monitoring orchestra state before starting this session.
- Preserving existing dirty application work; this session is scoped to skill-system files.
- Visual UI package will be adapted into SmartSpecPro-native form instead of installed raw, because the raw package contains optional `.venv` and external API integration material.

## 2026-04-26 — Visual UI Integration Strategy
Risk: MEDIUM
Mode: auto_by_default

- Added `visual-ui-enhancement` as a repo-local skill folder under `skills/`.
- Excluded the optional OpenAI Agents Python integration from the active package because SmartSpecPro skills should not depend on `.venv` or external LLM API wiring.
- Added seven CMD-12 visual UI agents and matching `.claude/agents/ssp-*` native definitions.
- Added behavior scenario validation as Level 1 routing coverage for skill-system changes.
- Added `visual-ui-enhancement` to the installed-skill mirror manifest so publish/verify includes the new skill.
- Updated runtime artifact cleanup to remove untracked `uv.lock` generated by `uv run` while preserving tracked lockfiles.
