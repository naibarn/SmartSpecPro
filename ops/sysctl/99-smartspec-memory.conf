# Keep the host responsive under transient application pressure.  Workloads
# have explicit cgroup limits; this only makes the kernel less eager to evict
# active pages into swap before those limits can contain a runaway worker.
vm.swappiness = 10
# Redis persistence/forking requires the kernel to allow safe overcommit.
vm.overcommit_memory = 1
