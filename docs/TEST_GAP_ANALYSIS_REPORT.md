# 🔍 Test Coverage Gap Analysis & Improvement Roadmap

รายงานฉบับนี้วิเคราะห์ส่วนที่ยังไม่ครอบคลุมการทดสอบในโครงการ SmartSpecPro และเสนอแนวทางเชิงกลยุทธ์เพื่อเพิ่ม Test Coverage ให้ถึงระดับสูงสุด (90%+)

---

## 📊 1. Gap Analysis (สถานะปัจจุบัน)

จากการตรวจสอบโครงสร้างโครงการ พบส่วนที่ยังขาดการทดสอบดังนี้:

### 🦀 Backend (Rust)
- **Commands Layer:** ไฟล์ `*_commands.rs` ทั้งหมด (ประมาณ 15 ไฟล์) ยังไม่มีการทดสอบ IPC interface
- **Complex Services:** `llm_service.rs`, `memory_manager.rs`, และ `plugin_system.rs` มี logic ที่ซับซ้อนแต่ยังขาด unit tests ที่ครอบคลุม edge cases
- **External Integrations:** `git_workflow.rs` และ `docker_manager.rs` ยังไม่มีการ mock ภายนอกเพื่อทดสอบ

### ⚛️ Frontend (TypeScript/React)
- **UI Components:** ไฟล์ในโฟลเดอร์ `components/` กว่า 96 ไฟล์ยังไม่มีการทดสอบการ Render และ User Interaction
- **Business Logic in Services:** Services กว่า 29 ไฟล์ (เช่น `chatService.ts`, `jobService.ts`) ยังไม่มีการทดสอบการจัดการข้อมูลและสถานะ
- **State Management:** การทดสอบการทำงานร่วมกันระหว่าง Hooks และ Stores (Zustand) ยังไม่ครอบคลุม

---

## ⚠️ 2. Hard-to-Test Areas & Solutions

| พื้นที่ที่ทดสอบยาก | สาเหตุ | แนวทางแก้ไข (Solution) |
|-------------------|-------|----------------------|
| **LLM Streaming** | ผลลัพธ์ไม่แน่นอนและเป็น Stream | ใช้ **Mock Server** (เช่น `mockito` ใน Rust) เพื่อจำลอง Stream response |
| **OS Keyring** | ต้องใช้สิทธิ์ระดับระบบ | ใช้ **Abstraction Layer** และ Mock keyring interface ในระดับการทดสอบ |
| **Tauri IPC** | ต้องรันในสภาพแวดล้อม Tauri | ใช้ **Command Mocking** ในระดับ Frontend และ Unit test logic แยกจาก Command wrapper |
| **WASM Plugins** | ต้องมี Runtime สภาพแวดล้อม | สร้าง **Minimal WASM Mock** สำหรับรันใน Test suite |

---

## 🚀 3. แผนการเพิ่ม Test Coverage (Phase 2: Advanced Testing)

### 🔹 ระยะที่ 1: Backend Logic Deep Dive (สัปดาห์ที่ 1)
- **Mocking External APIs:** ใช้ `mockito` เพื่อทดสอบ `llm_service.rs` โดยไม่ต้องเรียก API จริง
- **Database Integration Tests:** ใช้ SQLite in-memory สำหรับทดสอบ `workspace_data.rs` และ `cost_persistence.rs`
- **Error Path Testing:** เพิ่มการทดสอบกรณี Network fail, Disk full, และ Invalid API keys

### 🔹 ระยะที่ 2: Frontend Component & Service Testing (สัปดาห์ที่ 2)
- **Vitest + React Testing Library:** เริ่มเขียน tests สำหรับ `components/common/` (Button, Input, Modal)
- **Service Mocking:** ใช้ `vi.mock()` เพื่อจำลองการเรียก Tauri commands ใน `services/`
- **Hook Testing:** ใช้ `@testing-library/react-hooks` เพื่อทดสอบ `useAsync` และ `useDebounce`

### 🔹 ระยะที่ 3: Integration & E2E (สัปดาห์ที่ 3)
- **Playwright Setup:** เริ่มเขียน E2E tests สำหรับ Critical Path (Login -> Create Workspace -> Start Chat)
- **Cross-platform Tests:** รัน tests บนสภาพแวดล้อมจำลองที่ต่างกัน (Linux/Windows/macOS) ผ่าน GitHub Actions

---

## 📈 4. เป้าหมายใหม่ (Target Metrics)

| Metric | ปัจจุบัน | เป้าหมาย (Target) |
|--------|---------|-----------------|
| **Overall Coverage** | ~81% | **92%** |
| **Critical Services** | ~60% | **100%** |
| **UI Components** | ~10% | **70%** |
| **E2E Scenarios** | 0 | **15+ Scenarios** |

---

**รายงานโดย:** Manus AI Agent
**วันที่:** 14 มกราคม 2026
**Repository:** https://github.com/naibarn/SmartSpecPro
