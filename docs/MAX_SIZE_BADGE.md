# Max Size Badge

Publishes `badges/max-size.json` (Shields endpoint) on the `badges` branch.

## Meaning

Heuristic max upload/body size derived from `control-plane/src/config.ts`.

Example message:
- `max:50MB`
