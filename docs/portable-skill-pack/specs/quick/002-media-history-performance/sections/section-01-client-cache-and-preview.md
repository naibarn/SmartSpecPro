# Section 01 — client cache and preview

- Add exported cache constants/options-friendly values in `MediaHistory.tsx`.
- Configure task and Library queries to show previous data, revalidate on mount, and avoid focus refetch.
- Add lazy/async image loading and viewport-gated video metadata loading for list/gallery previews only.
- Add focused assertions without changing the existing media URL or orientation/title helpers.
