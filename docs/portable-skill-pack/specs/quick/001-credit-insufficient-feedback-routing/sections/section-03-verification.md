# Section 03: Verification

Ownership: focused tests and static checks.

Run the changed server test files, `cd apps/web && pnpm check`, and `git diff --check`. Report baseline failures separately from focused failures. Browser, live provider, deployment, and authenticated production checks are out of scope.
