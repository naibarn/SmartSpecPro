# Section 02 — Router Recovery

## Ownership

Thread variant context and fail early with a stable recoverable precondition.

## Targets

- `apps/web/server/routers/verticalDramaCharacters.ts`
- router tests

## TDD

Cover preview, direct image, sheet, and candidate entry points. Assert no paid
prompt call occurs for the base-adult child mismatch.

## Acceptance

The router returns a safe `PRECONDITION_FAILED` marker with no raw provider or
stack details, while age-stage variants proceed with child visual context.

## Risks

Keep every DB lookup owner/tenant scoped and preserve the existing credit and
worker authorization boundaries.
